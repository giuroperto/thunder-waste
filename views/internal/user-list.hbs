<h1>USERS</h1>
<ul>
  {{#each clients}}
  <li>
    <a href="/users/{{this._id}}">{{this.name}} | </a>
    <a href="/users/{{this._id}}/edit">Edit |</a>
    {{#if admin}}
    {{!-- add delete button --}}
    {{/if}}
    </li>
  {{/each}}
</ul>